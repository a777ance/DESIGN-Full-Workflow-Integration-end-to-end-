# NARF — review — 2026-06-16 — pass 1

I'll start by reading the key files to verify current state before recommending anything.

Both reads confirm the documented state. Let me verify the privacy-fallback gap (TD-14) details and the pricing/dues open items haven't shifted by checking the MARKETING README directly.

TD-14 confirmed exactly as documented: line `local-reason: ["cloud-gpu-reason", "cloud-overflow"]` — a sensitive task pinned to `local-reason` can fail over to `cloud-overflow` (Claude cloud). The config comment even *claims* the gate pins sensitive tasks local, but the failover map contradicts it. That's a real fail-open privacy gap.

I have enough to run the review.

---

# Portfolio Review — 2026-06-16

## Headline

Nothing material has shipped in the **9 days** since the last update (2026-06-07). Every Phase-1 critical-path item is still parked behind the same single dependency: **t630 access**. That's not a roadmap problem — it's an execution problem, and it's worth naming bluntly. The architecture is done; the box hasn't been touched.

The one *new* thing I'd flag hard: **TD-14 (privacy fail-open) is still Open**, and it's not just a deploy gap — it's a claim-vs-reality gap that becomes a liability the moment anyone runs the orchestration layer or repeats the "nothing sensitive leaves the box" promise to a customer.

---

## Top 3 Actionable Items (in order)

### 1. Deploy the nftables accounting layer to the t630 — *execution, not engineering*
**File/commands:** `localDNS/docs/statements/tools/collect/` →
```bash
sudo nft -f nftables-accounting.nft
sudo nft list table inet a777acct          # verify sets + counters exist
sudo python3 populate_sets.py --apply      # program the IP-sets
# then install the two cron lines (6h populate / nightly collect)
```
**Why:** This is the single highest-leverage move. It clears TD-03, unblocks TD-08, and lights up the Traffic Allocation donut + Household Profile sections that currently render from sample data. The code ships and is idempotent. **Blocked only on SSH to 192.168.1.118.**

### 2. Generate the first real end-to-end Statement for HH-0001 (Dave)
**Files:** `localDNS` generator → `customers` repo via `--data-dir`/`--out-dir`.
**Why:** Three of four data sources (Pi-hole, Uptime Kuma, `wg show`) are real the moment the box is up — you do **not** need to wait for the donut to ship an honest first Statement. The generator self-scopes (commit `8dcb7fe`), so empty sections drop out cleanly. Run `collect_stats.py` on the box → composes the household JSON → render. This is the actual Phase-1 gate deliverable ("pipeline tested on a real household end-to-end"). Bundle it into the same t630 visit as #1.

### 3. Close TD-14 — make the privacy fallback fail *closed*
**File:** `localDNS/10-ai-orchestration/config.yaml`, `fallbacks:` block.
**Decision I'd make:** change
```yaml
- local-reason: ["cloud-gpu-reason", "cloud-overflow"]
```
to a **local-only chain** — e.g. `["local-smart", "local-fast"]` — so a sensitive prompt fails to a smaller *local* model, never to Claude cloud. The config's own comment claims sensitive tasks are pinned local; the failover map silently breaks that. This is a config edit, not a deploy, and it requires **no box access** — it can be done today, independent of items #1 and #2.

**Note:** this is the one item not gated on the t630, which is exactly why I'd do it *now* rather than wait for the next box visit.

---

## Blockers

1. **t630 access remains the entire Phase-1 critical path.** Items #1, #2, plus P2 security cleanup (TD-01 stale WG peers, TD-02 laptop key rotation, TD-05 upstream verify) are all downstream of one SSH/physical visit. **Recommendation: treat the next t630 session as a single batched checklist** — nftables load + populate + cron, `collect_stats.py` run, WG peer audit, key rotation, upstream verify. One visit clears five tracked items.
2. **TD-14 privacy fail-open (P1).** Verified in `config.yaml` today. No privacy guarantee can be honestly stated until this is fail-closed. Not blocked on anything — purely waiting on the edit.

**Tension worth holding (both true):**
- *Conservative pole:* Phase 1 needs exactly four things running on one box and one honest Statement out the door. Everything else — Odin/LangGraph supervisor, console, rented-GPU offload — is moat-building that doesn't move the 3-paying-customers gate. Keep it dull; ship the Statement.
- *Visionary pole:* the AI-orchestration layer is genuinely differentiating and is most of the recent commit energy. It's fine to build it *as reference* — but it must not (a) consume the box visit that the Statement needs, or (b) ship with the privacy gap, because "privacy-first local inference" is the brand. The CEO decides which pole this week; my read is the Statement is the gate-mover and the orchestration layer is correctly labeled "not deployed."

---

## Open decisions still blocking (unchanged, flagged for owners)
- **Pricing validation** (ADR-007) — only resolves when a real cohort renews. ZORT owns the money side; this is a customer-acquisition dependency, not a tech one.
- **Contractor vs. employee classification** + **"guild-certified" vetting** — both need a lawyer. **Budget impact — ZORT should weigh in** on legal spend before either Stage 09/10 item moves.
- These do **not** block items #1–#3.

Now updating the portfolio with today's date and status changes.
