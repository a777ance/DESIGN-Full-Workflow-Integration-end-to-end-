# NARF — review — 2026-06-18 — pass 1

I'll run the review. Let me first verify the two items I can't confirm from memory: TD-14's live config state (since it's the one P1 fixable without box access) and whether the GitHub issue-creation blocker still holds.

Both confirmed against live files. TD-14 is real and the fix is exactly as described. The runbook for the t630 session is intact. Writing the review and updating portfolio.md.

---

## Portfolio Review — 2026-06-18

### Verified this session
- **TD-14 is real, against the live `config.yaml`.** Line confirmed: `- local-reason: ["cloud-gpu-reason", "cloud-overflow"]`. A `sensitive` task pinned to `local-reason` will, if the local model is cooled down (`allowed_fails: 2` → `cooldown_time: 60`), fail **open** to `cloud-overflow` = `anthropic/claude-opus-4-8`. The file *asserts the opposite* in two comments ("a `sensitive` task never routes here… pinned local first" and "a cloud→cloud fallback can't leak a private lookup"). Both claims depend on the LangGraph supervisor's gate — **which is not deployed.** At the LiteLLM layer, the gate does not exist. The privacy promise is currently false in config.
- **t630 runbook intact** (`docs/statements/tools/collect/README.md`). Three of four data sources work the moment the box is up; only the volume/category layer needs the nftables session. The exact commands are documented and idempotent.

---

### Top 3 actionable items right now

**1. Fix TD-14 — fail closed on the privacy fallback. `localDNS/10-ai-orchestration/config.yaml`. Do it today.**
This is the only P1 fixable without box access — a one-line edit in the `fallbacks:` map:
```yaml
# change:
    - local-reason: ["cloud-gpu-reason", "cloud-overflow"]
# to (local-only — fail closed):
    - local-reason: ["local-smart", "local-fast"]
```
Rationale: a false privacy guarantee is worse than no guarantee. The config and the README both print "nothing leaves the box" — we must not ship a path that contradicts it. **Caveat I'm naming honestly:** this trades graceful degradation for a hard stop — if all three local tiers are down, a sensitive reasoning task now *errors* instead of completing on cloud. That is the correct trade for a privacy product. Also update the two now-false comments so the file stops asserting a guarantee the deployed layer doesn't enforce.

**2. Schedule ONE t630 session and bundle every box-dependent item into it. `localDNS` + `customers`. (SSH 192.168.1.118)**
Not five tasks — one operator visit. Runbook: `docs/statements/tools/collect/README.md`.
- `sudo nft -f nftables-accounting.nft` (load ruleset)
- `sudo python3 populate_sets.py --apply` + install the 6h cron and nightly `collect_stats.py` cron
- Produce the first real `stats.json` for HH-0001 → unblocks TD-03, TD-08, TD-05, TD-13 (PWA test on a real Statement), and the first real Statement end-to-end
- While you're in: clean up WG peers 10.8.0.4–6 (TD-01) and rotate the laptop key (TD-02)

This is THE Phase-1 critical path. Four review cycles now with no real Statement, and the root cause is access cadence, not backlog.

**3. Decide what the $50 dues *unlock*. `MARKETING` — decision, not work.**
Amount is set (ADR-005); inclusions are still `CHANGE_ME`-shaped and block an honest operator pitch. My recommendation holds: dues buy (a) Alliance listing + match priority, (b) shared tooling tier, and (c) explicitly **NOT** bonding/background-check coverage yet — that's tied to the unresolved contractor-vs-employee classification. **Budget + compliance impact — ZORT should weigh in** before we imply any coverage we don't carry.

---

### The two poles worth naming (the oscillation)

On TD-14, the tension is instructive:
- **Conservative pole:** Defense-in-depth says the privacy guarantee must hold at *every* layer independently — the config must fail closed on its own, not lean on a supervisor that isn't running. Fix the config today. This is compressed institutional memory: "fail open" is how privacy products quietly leak.
- **Visionary pole:** The *intended* architecture — Heimdall, a deterministic privacy gate above the front door enforcing `allow_cloud=False` structurally — is the better long-term answer. A web of community resolvers with a provable local-only inference gate is a genuine public-good architecture. But it is **not deployed**, and a guarantee that only exists in un-run code is not a guarantee.

The CEO doesn't have to choose between them: **ship the config fix today (conservative) AND keep the supervisor gate on the roadmap (visionary).** Both are true. The config fix is not a substitute for Heimdall; it's the floor underneath it.

---

### Blockers

1. **t630 access is the #1 business risk to Phase 1.** Everything real is downstream of one SSH session. If founder access to the box is intermittent, that — not the backlog — is why Phase 1 isn't closing. Surfacing this to the CEO as the top operational risk, per fiduciary duty.
2. **Two decisions need humans, both with budget/compliance impact:** contractor-vs-employee (lawyer) and "guild-certified" vetting standard (legal). ZORT + CEO. The dues-inclusions decision partly depends on the classification answer.
3. **GitHub issue creation may still be blocked** (`403 Resource not accessible by integration` on `localDNS`, no `issues:write`). I have not re-tested it this session. The two ready-to-file issues (TD-14 fix; t630 deploy session) are drafted above and can be filed the moment the scope is granted — or copied in manually.

Now updating portfolio.md with today's date and status changes.
