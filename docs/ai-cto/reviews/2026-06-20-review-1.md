# NARF — review — 2026-06-20 — pass 1

I'll run the review. Let me verify the live state of the two items that have been sitting longest — TD-14's config and the t630 deploy runbook — before I restate them as actionable.

Both items confirmed against live files. Here's the review.

---

## Portfolio Review — 2026-06-20

### Headline

**No new CHANGELOG entry since 2026-06-07. That's now 13 days and a fourth review cycle with zero real Statements shipped.** The cause hasn't changed: everything real is downstream of one SSH session to the t630, plus two human decisions. This is not a backlog problem. It's an access-cadence and decision-cadence problem — and at this point that *is* the #1 business risk to Phase 1. The phase gate is still **0/5**.

I want to name something uncomfortable as your fiduciary: TD-14 — a verified false-privacy gap — has been the "do it today" item since 2026-06-17 and is **still open three review cycles later**. It needs ~3 lines and no box access. If it isn't getting done, the bottleneck isn't capacity, it's that these reviews aren't translating into commits. I've made the fix copy-pasteable below to remove every excuse.

---

### Top 3 actionable now

**1. Close TD-14 — fail the LLM-router privacy fallback closed. (`localDNS/10-ai-orchestration/config.yaml`, ~3 lines, no box access)**

Confirmed against the live config I just read. Line:
```yaml
- local-reason: ["cloud-gpu-reason", "cloud-overflow"]
```
A `sensitive`-tagged task pins to `local-reason`. If the local model is cooled down (`allow_fails: 2`, `cooldown_time: 60`), LiteLLM follows this chain → `cloud-gpu-reason` → `cloud-overflow` = `anthropic/claude-opus-4-8`. **A private home lookup leaks to Claude cloud whenever the local box stutters.** The `allow_cloud=False` guard lives only in the un-deployed LangGraph gate, not at the failover layer — so right now, nothing enforces it. The config's own comments assert the opposite three times. A false privacy claim is worse than no claim.

The exact edit:
```yaml
    - local-reason: ["local-smart", "local-fast"]   # fail CLOSED — never cloud for a sensitive task
```
And delete the now-orphaned comment block that says "light → heavy GPU → cloud" degradation applies to `local-reason`. Leave `cloud-gpu-reason: ["cloud-overflow"]` as-is — that chain is only reachable by non-sensitive tasks. **This is the single highest-value thing you can ship today, from any machine. Do it before anything else.**

**2. Book ONE t630 session and bundle every box-dependent item into it. (`localDNS` + `customers`, SSH `192.168.1.118`)**

Not five tasks — one operator visit. Runbook is `docs/statements/tools/collect/README.md` (I re-read it; it's complete and the commands are copy-ready). In order:
- `sudo nft -f nftables-accounting.nft` then `sudo nft list table inet a777acct` to confirm sets exist (TD-03)
- `sudo python3 populate_sets.py --apply` and install the two cron lines from the runbook (6h populate + nightly collect)
- Produce the first real `stats.json` for HH-0001 (Dave) → run `compose.py` → first real Statement end-to-end (closes 2 of 5 gate items)
- While you're in: clean up WG peers 10.8.0.4–6 (TD-01) and rotate the exposed laptop key (TD-02)

One trip unblocks TD-03, TD-08, TD-05, two phase-gate checkboxes, and two security items. **If founder box-access is genuinely intermittent, that's the constraint to fix first — name it to yourself as a calendar problem, not an engineering one.**

**3. Decide what the $50 dues *unlock*. (`MARKETING`, decision not code)**

Amount is locked (ADR-005); inclusions are still `CHANGE_ME`-shaped, which blocks an honest operator pitch. **NARF recommends:** dues buy (a) Alliance listing + match priority, (b) shared tooling tier, and (c) explicitly **NOT** bonding/background-check coverage yet — because that's downstream of the unresolved contractor-vs-employee classification. **This has a compliance + budget impact — ZORT should weigh in** before we imply any coverage we don't actually carry. Implying bonding we don't have is a fiduciary exposure, not just a marketing gap.

---

### Blockers

1. **t630 access is the Phase-1 critical path** — four cycles, no real Statement, all downstream of one SSH session. Top item #2 and security items TD-01/TD-02 all live here.
2. **Two human-only decisions with budget/compliance impact:** contractor-vs-employee (lawyer) and "guild-certified" vetting standard (legal). Both gate Stage 09/10; the first also gates dues-inclusions (#3). ZORT + CEO.
3. **GitHub issue creation still blocked** — integration lacks `issues:write` on `localDNS` (`403 Resource not accessible by integration`). The `issues` session mode can't function until the CEO grants that scope. TD-14 and the t630 session are both ready to file the moment it's fixed.

---

### The two poles, named (not flattened)

- **Conservative:** TD-14's fix *is* the canon — fail closed on a security boundary, every time. A privacy guarantee that depends on an un-deployed component isn't a guarantee. Ship the 3-line fail-closed edit and stop claiming privacy you can't enforce.
- **Visionary:** the deeper structure is that the LangGraph gate (Odin/Heimdall) should be the *only* thing that ever decides cloud-vs-local — the LiteLLM `fallbacks:` map shouldn't carry safety semantics at all. Long-term, strip all privacy logic out of `config.yaml` and let the supervisor own it as a single source of truth. Today's fix is the patch; that's the architecture.

The CEO picks which pole to fund. Today's edit holds either way.

Now updating portfolio.md with today's date and status.
