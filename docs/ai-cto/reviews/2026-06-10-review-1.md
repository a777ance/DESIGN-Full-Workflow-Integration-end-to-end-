# NARF — review — 2026-06-10 — pass 1

I'll start by reading the key files to ground this review in current state — the changelog (single source of truth for what shipped), the tech-debt detail, and the localDNS deploy runbook referenced in the top items.

I have enough to run a tight review. The runbook confirms the deploy path; the changelog confirms the privacy gap (TD-14). Let me write the review.

---

# Portfolio Review — 2026-06-10

## State check: nothing material shipped since 2026-06-07

The changelog's newest entry is still the 2026-06-07 LLM-router orchestration landing. No deploy, no first real Statement, no t630 access logged in three days. **The Phase-1 critical path has not moved.** That's the headline: the work isn't blocked on engineering or decisions — it's blocked on someone getting hands on the box.

---

## Top 3 actionable items right now

### 1. Deploy the nftables accounting layer to the t630 — `localDNS`
**This is execution, not engineering.** The runbook (`docs/statements/tools/collect/README.md`) is concrete and idempotent. The exact sequence:

```bash
ssh 192.168.1.118
cd ~/a777ance/collect
sudo nft -f nftables-accounting.nft           # load ruleset (re-runnable)
sudo nft list table inet a777acct             # verify sets + counters exist
sudo python3 populate_sets.py --apply         # program the IP-sets
# then install the two cron lines from the runbook (6h refresh + nightly collect)
```

**Why first:** It unblocks TD-03, TD-08, and the volume/profile sections of the first real Statement in one visit. Set elements time out in 24h, so the **cron must be installed in the same session** — a one-time `--apply` decays to zero within a day. That's the most common way this gets "deployed" but silently goes dead.

### 2. Run `collect_stats.py` for HH-0001 and generate the first real Statement — `localDNS` + `customers`
Three of four data sources (Pi-hole FTL, Uptime Kuma, `wg show`) are **real the moment the box is up** — you do not need nftables to produce a first honest Statement. The generator self-scopes (commit `8dcb7fe`), so it omits the volume donut until #1 lands. Sequence, same SSH session:

```bash
python3 collect_stats.py --out /var/lib/a777ance/2026-06.stats.json
# then render privately into the customers repo:
# generator --data-dir <customers/households/HH-0001-dave> --out-dir <…/statements>
```

This produces a **real value receipt for Dave this month** — honest by construction, volume section dark until #1 is fed. That is the single most important Phase-1 artifact: proof the pipeline works end-to-end on a real household.

### 3. Fix TD-14 — the LLM-router privacy fail-open — `localDNS`
This is the one item that is **not** gated on t630 access; it's an editable config change. A `sensitive`-tagged task routes to `local-reason`, which falls back to `cloud-overflow` (Claude cloud) if the local model is down. **A sensitive prompt can leak to a cloud provider** — and the dispatcher's own docstring promises a local-only chain. Fix: give `local-reason` a local-only fallback (fail closed) in `config.yaml`, or remove its cloud fallback entirely.

**Why it's on the top-3 despite the router not being deployed:** This is a documented privacy guarantee that the code currently violates. The fiduciary call is to close it *before* the router ever runs live, not after. It's cheap, it's local, and it removes a latent "we said we'd never send sensitive data to the cloud" liability. Do it now while it's a one-line config change and not an incident.

---

## Blockers

**B1 — t630 access is the entire Phase-1 critical path (unchanged, now the dominant risk).**
Items #1 and #2 above, plus P2 security cleanup (TD-01 stale WG peers, TD-02 exposed Windows key), all require SSH/physical access to `192.168.1.118`. Three days with no movement suggests access is intermittent. **If that's structural, it is the real bottleneck and should be named to the CEO as such** — every "real" thing in Phase 1 sits downstream of one box you can't always reach. Recommendation: **bundle everything into a single planned visit** — deploy nftables (#1), collect stats + generate Statement (#2), and clear TD-01/TD-02 security debt while you're in there. One trip clears four items.

**B2 — TD-14 privacy fail-open (open, actionable now).** Covered above. Not access-gated.

**Not blockers (correctly de-scoped):** Cohort data for "How You Compare" blocks one *section*, not the Statement. Dues inclusions, vetting standard, contractor classification — all Phase-2-gate items, none block the next move.

---

## The tension worth naming

**Conservative pole:** The boring play is right. Deploy the shipped code, collect real numbers from a real home, hand Dave an honest receipt. Don't touch the orchestration layer beyond closing the privacy hole. Phase 1 is a sales problem now, not an engineering one — the stack is built, it needs to be *run*.

**Visionary pole:** The Odin/LangGraph supervisor and the AI-orchestration stack are genuinely interesting — a privacy-gated local-first agent mesh is a real differentiator nobody in home-network services has. **But it is exploratory, and Phase 1 has zero paying customers.** Every hour on Odin is an hour not spent proving liquidity. The visionary work is a gift *labeled as exploratory* — it does not enter the critical path until the Phase-1 gate clears.

The CEO decides which pole to inhabit. My recommendation for the next two weeks: **conservative.** Get one real Statement into one real customer's hands. That's the whole game right now.

---

Now updating portfolio.md with today's date and status changes.
