# NARF — review — 2026-06-06 — pass 1

I'll run the review. Let me pull the live state on the items most likely to have shifted since the last reconcile — the t630 deploy path, the customers pipeline, and the two CEO overnight questions sitting in MARKETING.

I have what I need. The deploy checklist is fully specified, the dues decision is resolved (the portfolio is stale on that point), and there are two CEO overnight questions sitting in MARKETING that the last review didn't address. Let me write the review.

---

# Portfolio Review — 2026-06-06

## State check first

I re-read the live source files. Two things the portfolio is now **stale** on, plus two open CEO questions the last review never touched:

- **Top item #3 ("Lock the member dues amount") is already done.** ADR-005 set it to $50/mo on 2026-06-04, and `MARKETING/README.md` reflects it. The portfolio's "Top 3" still lists it as open work. That's a slot wasted — I'm replacing it below.
- **The nftables deploy is now a fully-written runbook.** `CLAUDE.md` section F is a copy-paste, 5-step, single-SSH-session checklist. This is no longer "figure out how" — it's "get on the box." That hardens my read that **t630 access is the entire Phase-1 critical path**.
- **Two CEO overnight questions** (lead-gen channel, CRM tool) are logged in `MARKETING/docs/ai-cto/context.md` and were never answered. I address them at the bottom — but they don't displace the build items.

---

## Top 3 actionable now

### 1. Deploy the nftables accounting layer to the t630 — `localDNS`
This is unchanged as #1 and it's the right call. The runbook exists (`CLAUDE.md` §F + `collect/README.md`). It is one SSH session:

```bash
scp -r docs/statements/tools/collect/ user@192.168.1.118:~/a777ance/collect/
sudo nft -f ~/a777ance/collect/nftables-accounting.nft
sudo python3 ~/a777ance/collect/populate_sets.py --apply
sudo nft -j list counters table inet a777acct   # non-zero within minutes = success
```
Then the two cron lines. **One caveat to flag from the runbook:** the cron `populate_sets.py` line uses `sudo` inside crontab — that needs either a root crontab or a NOPASSWD sudoers entry for that exact script, or it'll fail silently at 3am. Decide that on the box. Unblocks TD-03, TD-08, TD-05 (verify upstream same visit), and lights up the volume + profile sections of the Statement.

### 2. Generate the first real end-to-end Statement for HH-0001 (Dave) — `localDNS` + `customers`
Three of the four data sources are real the moment the box is up (Pi-hole FTL, `wg show`, Uptime Kuma). The generator self-scopes (`8dcb7fe`) so it's honest by construction. The only thing standing between us and a real Statement is **running `collect_stats.py` on the box** — which is the *same SSH visit as #1*. Do them together. This checks the last unchecked-but-checkable box on the Phase-1 gate: "pipeline tested on a real household end-to-end."

### 3. **NEW — Answer the CEO's two overnight questions so the lead-gen loop can start** — `MARKETING`
Dues is done, so this slot is free. The CEO is sitting on a go/no-go for tomorrow morning. These are decisions, not builds, and they gate the only thing that matters in Phase 1 — **getting 3 paying customers**. My calls below. The CEO/CMO ratify.

---

## The two CEO questions — my recommendations

**Q1 — Lead-gen channel.** The CEO's hypothesis is sound. I'd sharpen it into a sequence and name the single first action:

- **First action tomorrow: claim the free Google Business Profile + Nextdoor business listing.** Both are free, both are hyperlocal, both take ~20 min. Zero budget impact. Do not wait on anything technical.
- **Week-by-week shape:** Weeks 1–2 = listings live + door-to-door on Dave's block (Dave = HH-0001 is your physical proof + voucher). Weeks 3–6 = convert door-knocks to founding-cohort installs. Week 6+ = the **QR referral loop** turns on — but only *after* a real Statement is delivered (item #2). **Technical dependency, flagged:** the "Connect in the Alliance" QR loop cannot generate leads until Statement delivery is live. So items #1/#2 are *upstream of the best free channel you have.* That's the real sequencing insight.
- **Tension worth naming:** the *conservative* play is door-to-door + free listings — proven, zero-cost, density-building, exactly right for hyperlocal home services at N<5. The *visionary* play is leaning into "pest control for your network" as a category you own with local SEO content — slower to pay off, but it compounds and it's a moat cheap-tech competitors can't copy. Both are true. I'd run the conservative pole now and seed the visionary one in parallel at zero marginal cost. **Skip paid ads pre-validation** — agreed with the CEO, and ZORT will back that.

**Q2 — CRM.** Confirmed: **Google Sheets for Phase 1, no blockers.** The schema in `08-client-list-and-crm/schema.md` should drive this, not the tool. One concern to flag honestly: there's a latent **two-sources-of-truth risk** between `customers/roster.json` (technical/live roster) and a CRM sheet (business facts). Keep the boundary ADR-006 already drew — `roster.json` is network truth, the Sheet/Stage-08 master list is business truth, no field duplicated across both. On Airtable for Phase 2: I have **not** yet verified its free tier against the `DESIGN/11-automations/` Zapier/Make plans — that's a real open question (it ties to TD-06) but it is a *Phase 2* question and does not block anything now. I'll leave it flagged, not answered, rather than guess.

---

## Blockers

1. **t630 access remains the single Phase-1 critical path.** Items #1, #2, and the P2 security cleanup (TD-01 stale WG peers, TD-02 key rotation, TD-05 upstream verify) all sit behind SSH to `192.168.1.118`. **Bundle all of it into one visit.** If founder box-access is intermittent, *that* is the true bottleneck — everything real in Phase 1 is downstream of one SSH session. Nothing here is engineering anymore; it's all execution.

2. **Compliance decisions still need a lawyer — budget impact, ZORT should weigh in.** Contractor-vs-employee classification (Stage 10) and the "guild-certified" vetting standard (Stage 09) both require legal review. These don't block the *first 3 customers* (founder wears the operator hat), but they block recruiting a *real* operator — which is a Phase-1 gate item. Don't let them surprise us at the gate. **This has a budget impact (legal fees) — ZORT should size it.**

No new blockers. No new tech debt.

Let me record this.
