# NARF — review — 2026-06-11 — pass 1

I'll run the portfolio review. Let me pull the current state of the key files to make sure I'm working from live facts, not the snapshot.

The runbook and changelog confirm the snapshot is accurate. Everything real in Phase 1 is downstream of t630 access. Here's the review.

---

## Portfolio Review — 2026-06-11

### State of play
Phase 1 (prove liquidity). The thread running through every blocker is unchanged and worth stating bluntly: **almost nothing real ships until someone has SSH/physical access to the t630 at 192.168.1.118.** The repos are healthy config snapshots; the box is where value gets created. That is the bottleneck, not engineering.

One new fact surfaced since the last review that I'm treating as a fiduciary-grade disclosure: **TD-14 is a real privacy-promise gap, not a theoretical one.** It's worth a hard look (below).

---

### Top 3 actionable right now

**1. Deploy the nftables accounting layer to the t630 — execution, not engineering.**
The code ships and the runbook is exact. On the box:
```bash
sudo nft -f nftables-accounting.nft
sudo nft list table inet a777acct            # verify sets + counters exist
sudo python3 populate_sets.py --apply        # program the IP-sets
# then install the two crons from collect/README.md (6h populate, nightly collect)
```
Unblocks TD-03, TD-08, and lights up the volume + profile sections of the Statement. File: `localDNS/docs/statements/tools/collect/README.md`. **Same visit, bundle the P2 security cleanup** (TD-01 identify/remove WG peers 10.8.0.4–6; TD-02 rotate the exposed Windows laptop key) — defense-in-depth, and you're already on the box.

**2. Generate the first real end-to-end Statement for HH-0001 (Dave).**
The generator self-scopes (commit `8dcb7fe` — omits any section without honest data), so month one is honest by construction even before nftables. The only gating step is real figures:
```bash
python3 collect_stats.py --out /var/lib/a777ance/2026-06.stats.json   # on the box
# then render into the private customers repo via --data-dir/--out-dir
```
Three of four data sources (Pi-hole, Uptime Kuma, wg) are real the moment the box is up. nftables (#1) adds the fourth. This is the single most important Phase-1 artifact — it's the value receipt, the salesperson, and the referral engine in one.

**3. Decide what the $50 dues *unlock* — a decision, not work.**
The amount is locked (ADR-005). What's still `CHANGE_ME` is the *inclusions*: tooling tier, match priority, bonding/background-check coverage. This blocks the Stage 09 operator pitch — you can't recruit against an undefined offer. My recommendation to write into `MARKETING/README.md`: dues unlock **(a) platform tooling + match priority, (b) brand/listing, (c) the matching engine** — and explicitly **carve out bonding/background-check coverage** until the vetting standard and contractor-vs-employee questions get legal review. **Budget + compliance impact — ZORT and a lawyer should weigh in before bonding is promised.**

---

### Blockers

1. **t630 access is *the* Phase-1 critical path** (unchanged, now sharper). Items #1 and #2 above, plus TD-01/TD-02/TD-03/TD-08, all sit behind one SSH session. If founder box-time is intermittent, that intermittency *is* the schedule. **Decision I'd make: treat the next t630 session as a single batched checklist** — nftables load + populate + cron, collect_stats run, WG peer audit, key rotation, verify Pi-hole upstream (TD-05). One visit clears six items.

2. **TD-14 — privacy fallback gap (P1). Fiduciary flag.** Today a `sensitive`-tagged task routes to `local-reason`, but `config.yaml` gives `local-reason` a fallback chain ending in `cloud-overflow` (Claude cloud). If the local model is down, **a sensitive prompt can fail over to a cloud provider** — directly contradicting the "nothing leaves the box" promise printed on every Statement. The orchestration layer is *not deployed*, so there is no live leak today — but this must be fixed **before any cloud tier is stood up on the box, and before that privacy line is used in a customer pitch.** Fix is small: give `local-reason` a local-only fallback (fail closed). I'm recommending an issue for this.

3. **TD-06 — Stage 11 automations (P1, DESIGN).** Cross-stage transitions need manual re-entry. Not on the critical path for the first Statement, but it's the friction that will bite once there's more than one household. Flagging it stays P1; not actioning it this week.

---

### What I'm explicitly *not* recommending
- No Phase 2 app work — the gate checklist is empty (0/5). Holds.
- No Azure-lab work — scope still undefined (TD-10).
- No standing up the AI gateway / Odin / console on the box yet (all P3) — and **especially** not the cloud AI tiers until TD-14 is fixed. Here's the tension worth naming: the AI-orchestration layer is genuinely interesting architecture (edge-first, privacy-gated, vendor-independent — exactly the "AI-native homelab" direction). *And* it earns zero Phase-1 dollars and currently carries a privacy gap. Both true. The conservative pole wins this quarter: ship the Statement, prove three customers, leave Odin in the design drawer. The CEO can choose to inhabit the visionary pole — but not before the gate clears.

---

Let me record this review in the portfolio.
