# NARF — review — 2026-06-05 — pass 1

I'll run the review. Let me pull the live state of the two repos that drive Phase 1 to make sure I'm not reviewing stale assertions.

Both files confirm the hub's assertions are accurate. The collect README is especially clarifying — it makes the t630 dependency precise. Here's the review.

---

## Portfolio Review — 2026-06-05

### State check: the hub is honest

I verified the two Phase-1-critical claims against live files. Both hold:

- **`collect/README.md`** confirms the nftables layer is *shipped, not built* — three of four data sources (Pi-hole, Uptime Kuma, `wg show`) work the moment the box is up; only **volume-by-category** needs the populator running. This is execution, not engineering. Confirmed.
- **`MARKETING/README.md`** confirms dues ($50, ADR-005) and pricing ($175 + $35/mo, ADR-007) are set and written in. No stale `CHANGE_ME`. Confirmed.

The hub's reconcile from this morning is clean. I'm not finding drift.

---

### Top 3 actionable items right now

**1. Get on the t630 (`192.168.1.118`) and stand up the volume layer — and bundle the security cleanup into the same visit.**

This is the single most concrete action available. Everything real in Phase 1 is downstream of box access. The exact sequence (from `collect/README.md`), in order:

```bash
# a. load the accounting ruleset (idempotent)
sudo nft -f nftables-accounting.nft
sudo nft list table inet a777acct          # verify sets + counters exist, all zero

# b. program the sets from categories.json
sudo python3 populate_sets.py --apply

# c. schedule both — populator every 6h (CDN IPs time out in 24h), collector nightly
#    (cron block is in collect/README.md — edit USER path before installing)

# d. while you have the shell open — clear the P2 security debt:
sudo wg show                                # TD-01: identify or remove peers 10.8.0.4–6
                                            # TD-02: rotate the exposed Windows laptop WG key
```

Resolves: **TD-03** (deploy), unblocks **TD-08** (per-category volume), and closes **TD-01/TD-02** in the same visit. *Don't make two trips.*

**2. Generate the first real end-to-end Statement for HH-0001 (Dave).**

The generator self-scopes (commit `8dcb7fe`) — it omits any section without honest data. So the moment you've run `collect_stats.py` on the box (item 1 lights up volume; even without it, Pi-hole + Uptime Kuma + `wg show` are real *today*), you can render an honest Statement into the private `customers` repo via `--data-dir`/`--out-dir`. This is the Phase-1 gate line *"pipeline tested on a real household end-to-end."*

Sequence: run `collect_stats.py --out` on the t630 → pull `stats.json` into `customers/households/HH-0001-dave/stats/` → run `compose.py` → render. **Note the honesty dependency:** if you render *before* the nftables populator has had a few hours to fill the sets, the volume donut/profile sections will self-omit — which is correct, not broken.

**3. Answer the CEO's two overnight questions (in `MARKETING` context.md) — they're decisions, not work, and they're blocking the CEO's tomorrow-morning action.**

These were filed for tonight's review. My calls below.

---

### CEO overnight brief — my answers

**Q1 — Lead gen on limited capital.** The hypothesis is right. My one stress-test note and the single first action:

The four channels rank correctly, but they have a **technical dependency the CEO should know**: the QR referral loop (Statement → "Connect in the Alliance") **requires Statement delivery to be live first**. That's item #2 above. Until HH-0001's Statement is rendered and delivered, the cheapest growth channel (customer #1 referring #2–10) is dark. So the sequence is: *get the Statement live → then the referral loop becomes a real channel, not a slide.*

**Single first action tomorrow morning:** Claim the **Google Business Profile** (free, ~20 min, captures "home network help [city]" intent immediately) — *and* post the first **Nextdoor** business listing. Both are free, both are hyperlocal, neither has a stack dependency. Door-to-door is higher-conversion but rate-limited by the founder's time; do it *after* the Statement exists so there's a physical artifact to leave behind.

I'd hold the conservative line here: **skip paid ads pre-validation** — that's a budget question and **ZORT should weigh in** before any ad spend. The visionary pole: the Statement *is* the ad. A delivered Statement left on a neighbor's counter outperforms any CPC at this scale. Both true — but only one is free.

**Q2 — CRM.** The hypothesis is right, with one confirmation and one flag:

- **Phase 1 (≤5): Google Sheets — yes.** But the single source of truth for business facts is **Stage 08 schema** (`DESIGN/08-client-list-and-crm/schema.md`) and the live roster is `customers/roster.json`. *The Sheet must mirror that schema, not invent its own columns* — otherwise you fork the source of truth. Use the Sheet as a view, `roster.json` as canon.
- **Airtable free tier (Phase 2):** I did **not** verify the Stage 11 automation integration tonight — I read the Phase-1-critical files first. **Flag:** before committing to Airtable as the Phase-2 master list, someone must confirm the `DESIGN/11-automations/` Zapier/Make hooks work against Airtable's free-tier API limits (TD-06 is still open — those automations aren't wired yet, so there's nothing to test against). I'd open this as a tracked question rather than answer it blind. **Schema drives tool, not the reverse** — the CEO's instinct here is correct.

---

### Blockers

1. **t630 access is *the* Phase-1 critical path.** Items #1 and #2 above, plus TD-01/TD-02, all require SSH/physical access to `192.168.1.118`. If founder access is intermittent, *that* is the true bottleneck — name it as such. Everything real in Phase 1 is one shell session away.

2. **Referral loop is gated on Statement delivery** (newly surfaced from the CEO Q1 analysis). The cheapest lead-gen channel can't turn on until item #2 ships. Not a hard blocker — a sequencing constraint worth making explicit.

No other blockers. The DESIGN path-integrity and `customers` visibility blockers were correctly closed this morning.

---

Now updating the hub with today's date, the verified status, the CEO-brief resolutions, and the newly-surfaced referral-loop dependency.
