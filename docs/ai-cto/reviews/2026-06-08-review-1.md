# NARF — review — 2026-06-08 — pass 1

I'll review the portfolio. Let me pull the freshest context on the two areas where the state file looks like it may have drifted — the LLM-router privacy gap (TD-14) and the deploy-blocked items.

Let me confirm the privacy fix status by reading the actual config and supervisor — TD-14 is the only P1 with a genuine safety implication, so I want to know if it's still open before I rank it.

Confirmed. **TD-14 is still open in the config.** Line: `local-reason: ["cloud-gpu-reason", "cloud-overflow"]`. The comment claims it's "safe" because the supervisor pins sensitive tasks local — but that's the *supervisor's* gate. The LiteLLM `fallbacks` map is enforced **at the front door**, below the supervisor. Any client hitting `ai.home.lan:4040` directly with a `local-reason` request (or the dumb `dispatcher.py` switch, which has no supervisor) gets a silent failover to `cloud-overflow` (Claude cloud) when the local model is down. The config's own "Safe:" comment only covers the `cloud-*` tiers, not `local-reason`. The gap is real and the fix is one line.

Here's the review.

---

## Portfolio Review — 2026-06-08

### Top 3 actionable items right now

**1. Close TD-14 — the LLM-router privacy fallback gap (localDNS). One-line config fix, P1.**
This is the only open item with a genuine privacy-guarantee implication, and the fix is trivial relative to the risk. In `localDNS/10-ai-orchestration/config.yaml`, change:
```yaml
- local-reason: ["cloud-gpu-reason", "cloud-overflow"]   # leaks: both are reachable-cloud paths
```
to a **local-only, fail-closed** chain:
```yaml
- local-reason: ["local-smart", "local-fast"]            # fail closed; never cross the Bifröst
```
`cloud-gpu-reason` is also a cloud path (rented GPU over Tailscale — sworn, but still off-box), so dropping it from the sensitive tier's fallback is correct for a fail-closed posture. Then re-run `python3 supervisor.py --selftest` and add a selftest assertion that **no `local-*` tier has a cloud tier anywhere in its fallback chain** — so this can't silently regress. *Note:* this hardens a layer that is **not yet deployed** (no box access, no API key), so it's not a live breach today — but fixing it now means the guarantee is true *by construction* before anything routes through it. Don't claim "sensitive never leaves the box" in any customer-facing copy until this lands.

**2. Deploy the nftables accounting layer to the t630 (localDNS). The Phase-1 critical path.**
Unchanged from last review and still #1 on the value chain: code ships (ruleset + populator + runbook at `docs/statements/tools/collect/README.md`), remaining work is execution. SSH to `192.168.1.118`, load the ruleset, schedule `populate_sets.py`, verify counters. Unblocks TD-03, TD-08, and lights up the volume + profile sections of the first real Statement. **Everything real in Phase 1 is downstream of t630 access** — see blocker below.

**3. Generate the first real end-to-end Statement for HH-0001 (Dave) (localDNS + customers). The phase-gate proof.**
The generator self-scopes to honest sections (commit `8dcb7fe`), the private `customers` repo exists. The only blocker is running `collect_stats.py` on the t630 for real figures — same box-access dependency as #2. **Bundle #1, #2, and #3 into a single t630 session**: do the config fix, deploy nftables, collect stats, render the Statement. One visit clears three items.

### Blockers

- **t630 access (192.168.1.118) is the single Phase-1 critical path.** Items #2 and #3, plus P2 security cleanup (TD-01 stale WG peers, TD-02 rotate the exposed Windows key), all require SSH/physical access. If founder access is intermittent, *that* is the true bottleneck — name it honestly. One session can clear: nftables deploy, stats collection, Statement render, peer audit, key rotation.
- **No new path or data-source blockers** — the 2026-06-05 reconcile holds; hub files resolve, data flows as documented.

### Conservative / visionary tension worth naming (for the CEO, not to resolve)

The **Odin LangGraph supervisor** is genuinely impressive work — and it is **Phase 2+ architecture sitting in a Phase 1 repo**. Two poles, both true:
- **Conservative:** Phase 1's goal is *3 paying customers*. None of the orchestration layer ships value to a customer this quarter; the dumb `dispatcher.py` switch is the right Phase-1 default, and every hour on Odin is an hour not on the t630 deploy. Keep it boxed as reference code; don't deploy it until there's a real workload.
- **Visionary:** An on-prem, fail-closed, deterministic-privacy-gated AI fortress is a *real moat* and a future Statement differentiator no NextDNS/eero competitor can match — "your household AI that physically cannot phone home." That's worth having designed early so it's ready when liquidity is proven.
The CEO decides which pole to inhabit. My fiduciary flag: don't let the elegance of Odin pull a solo founder off the boring t630 deploy that actually clears the phase gate.

### Budget flag for ZORT
- The cloud tiers (`cloud-overflow`, `cloud-explore/code/vision`) all bill per token via `ANTHROPIC_API_KEY`, and `cloud-gpu-reason` implies a rented GPU (Vast.ai/RunPod). **None should be provisioned in Phase 1** — no customer benefit yet, real recurring spend. ZORT should weigh in before any API key or GPU pod is stood up. The local-only fix in #1 also *reduces* accidental cloud spend exposure (a fail-closed sensitive tier can't run up Claude bills on failover).

Now updating portfolio.md with today's date and the status changes.
