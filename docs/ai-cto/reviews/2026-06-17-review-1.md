# NARF — review — 2026-06-17 — pass 1

I'll run the portfolio review. Let me pull the current ground-truth on the items that matter most before I update anything.

Both top items still trace back to the same root dependency. Let me confirm the privacy-gap config (TD-14, the only P1 that's pure engineering, not box-access) so I can speak to it precisely.

Confirmed. TD-14 is real and exactly as described: line `local-reason: ["cloud-gpu-reason", "cloud-overflow"]` means a sensitive prompt fails open to Claude cloud if the local model is down. The config's own comments *claim* the gate pins sensitive tasks local — but that claim lives in the LangGraph supervisor, which isn't deployed, while the flat `dispatcher.py` is the documented default. The guarantee is asserted in three comments and contradicted by one fallback line.

Here's the review.

---

## Portfolio Review — 2026-06-17

### The shape of it
Nothing material has shipped since the 2026-06-07 LLM-router landing. The Phase-1 gate is still 0/5. The critical path hasn't moved because **it isn't an engineering problem — it's a t630-access problem and a CEO-decision problem.** Most of what's "open" is waiting on the founder, not on code. That's the honest headline, and it's worth saying plainly: a stalled review three times running means the bottleneck is access cadence, not backlog.

### Top 3 actionable items, in order

**1. Close TD-14 — the LLM-router privacy fallback gap (P1, and the ONLY P1 you can fix without the box).**
- **File:** `localDNS/10-ai-orchestration/config.yaml`, the `fallbacks:` block.
- **Concrete fix:** change `local-reason: ["cloud-gpu-reason", "cloud-overflow"]` so its chain stays local — e.g. `local-reason: ["local-smart", "local-fast"]` (fail closed / degrade local), and remove `cloud-overflow` from any chain a `sensitive`-tagged task can reach.
- **Why it jumps the queue:** every other P1 is blocked on physical access; this one is a three-line edit you can do today. It also closes a *correctness* gap — the config asserts "sensitive never reaches cloud" in three comments while one line makes it false. **A privacy promise that's wrong is worse than no promise**, because it's printed on the product's ethos. The visionary read: a deployed deterministic privacy gate (Heimdall) makes this structurally impossible — but Heimdall isn't deployed, so fail-closed config is the defense-in-depth you ship *now*.
- It's reference code, not deployed — so the urgency is "fix before anyone trusts the claim," not "production incident."

**2. Get one t630 session scheduled and bundle everything box-dependent into it.**
- **Why:** Items #1-deploy, the first real Statement, nftables, and the P2 security cleanup (TD-01 stale WG peers, TD-02 exposed laptop key) ALL sit behind SSH to `192.168.1.118`. This isn't five tasks; it's one visit with a checklist.
- **Concrete checklist for that visit:**
  - `sudo nft -f nftables-accounting.nft` then `sudo python3 populate_sets.py --apply` (runbook: `localDNS/docs/statements/tools/collect/README.md`)
  - Install the two cron lines from that runbook (6h populate, nightly collect)
  - `wg show` → identify or remove peers 10.8.0.4–6 (TD-01); rotate the Windows laptop key (TD-02)
  - `python3 collect_stats.py --out …` → produces the first real `stats.json`
- **The decision I'd make:** stop listing these as separate blockers. They're one operator-visit ticket. I'll create that issue.

**3. Resolve "what the $50 dues unlock" (decision, not work — `MARKETING`).**
- The amount is set (ADR-005). What's still `CHANGE_ME`-shaped is *inclusions* — tooling tier, match priority, bonding/background-check coverage. This blocks the operator pitch from being honest.
- **What I'd decide:** dues buy (a) listing + match priority in the Alliance, (b) the shared tooling tier, and (c) **not** bonding/background-check coverage yet — that's a legal/insurance question that touches the unresolved contractor-vs-employee classification. **Budget + compliance impact — ZORT should weigh in** before we promise operators anything that implies coverage, because an implied bond we don't carry is a liability.

### Blockers (unchanged but worth re-stating sharply)
1. **t630 access is THE Phase-1 critical path.** Three review cycles, no real Statement, because everything real is downstream of one SSH session. If founder access is genuinely intermittent, that's the single fact that decides whether Phase 1 closes — name it to the CEO as the #1 risk.
2. **Two decisions need humans, not me:** contractor-vs-employee (lawyer) and "guild-certified" vetting (legal). Both gate Stage 09/10 and both have budget impact — ZORT + CEO.

Let me create the two issues that have clear next actions, then update the portfolio.

Both issue creations failed — `403 Resource not accessible by integration`. The token lacks issue-write scope on `localDNS`. That's a **process blocker worth flagging**: I can't open issues right now, so the two tickets above exist only in this review. Noting it in the portfolio so it isn't silently lost — the CEO needs to either grant the integration `issues:write` or copy these two issues in manually.

Now updating the portfolio with today's date, the new findings, and the status changes.

Review complete. Summary:

## What I did
- Verified the two top items against ground truth (the collect runbook + live `config.yaml`), confirmed **TD-14 is real exactly as flagged**, and updated `portfolio.md` with today's date, the elevated TD-14, the bundled-t630-visit framing, and a new blocker.

## Top 3 actionable now
1. **Close TD-14 — fail closed** (`localDNS/10-ai-orchestration/config.yaml`). Three-line edit; the only P1 you can fix without the box. The config claims sensitive prompts never reach cloud, but one fallback line makes that false.
2. **Schedule ONE t630 session** and bundle nftables deploy + first real `stats.json` + WG peer cleanup (TD-01/02) into a single visit. These aren't five tasks — they're one ticket.
3. **Decide what the $50 dues unlock** (`MARKETING`) — recommend listing+match+tooling, *not* bonding yet. Budget/compliance angle → ZORT should weigh in.

## Blockers surfaced
- **t630 access is the real reason Phase 1 isn't closing** — three review cycles, no real Statement, all downstream of one SSH session. I named this to the CEO as the #1 Phase-1 risk.
- **Two human-only decisions** (contractor/employee, vetting standard) with budget impact — ZORT + lawyer.
- **New process blocker:** GitHub issue creation returned `403 — not accessible by integration`. **I could not file the two ready issues.** The integration needs `issues:write`, or the CEO copies them in manually. The `issues` session mode is non-functional until that's fixed.

The fastest real progress this week: do #1 from a keyboard today (no box needed), and lock a date for the single t630 visit. Everything else in Phase 1 is waiting on that visit.
