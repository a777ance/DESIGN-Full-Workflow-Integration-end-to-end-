# NARF — review — 2026-06-13 — pass 1

I'll run the review. Let me pull the current source-of-truth files to make sure the portfolio reflects live state before I update it.

The live state matches the portfolio. Here's the review.

---

## Portfolio Review — 2026-06-13

### The shape of it
Nothing material has shipped since the 2026-06-07 LLM-router orchestration landed. The portfolio is honest about that, which is good. But it means **Phase 1 has not moved in ~6 days**, and every real Phase-1 deliverable is still downstream of one thing: **physical/SSH access to the t630 at 192.168.1.118.** That is the whole story. Let me name it plainly rather than dress it up as three independent items.

### Top 3 actionable now

**1. Get on the t630 and deploy the nftables accounting layer.** *(unblocks TD-03, TD-08, TD-13, and the volume/profile sections of the first real Statement)*
This is execution, not engineering — the runbook is complete (`localDNS/docs/statements/tools/collect/README.md`). Concretely:
```bash
sudo nft -f nftables-accounting.nft
sudo nft list table inet a777acct        # verify sets + counters exist
sudo python3 populate_sets.py --apply    # program the sets
# then install the two cron lines from the runbook (6h populate, nightly collect)
```
Do this and the only "build" item left in Phase 1 collapses to "wait for a month of counter data."

**2. While on the box, run `collect_stats.py` for HH-0001 and generate Dave's first real Statement.** *(Phase-1 gate item: "pipeline tested on a real household end-to-end")*
Three of four data sources (Pi-hole, Uptime Kuma, WireGuard) are real the moment the box is up — you do **not** need to wait for a month of nftables data to ship a *first* honest Statement. The generator self-scopes (commit `8dcb7fe`): no volume → no donut, no cohort → no "How You Compare." So:
```bash
python3 collect_stats.py --out /var/lib/a777ance/2026-06.stats.json
# then render via localDNS generator with --data-dir/--out-dir into the private customers repo
```
This converts HH-0001 from "pipeline exists" to "real artifact in hand" — the thing the operator pitch and the referral loop both depend on.

**3. Fix TD-14 — the LLM-router privacy fail-open — before any privacy claim ships.** *(P1, and it's a fiduciary flag, not just tech debt)*
This is the one item **not** blocked on the t630, so it's the thing you can do without box access. In `localDNS/10-ai-orchestration/config.yaml`, `local-reason` falls back to `cloud-overflow` (Claude cloud). A `sensitive`-tagged prompt can leak off-box if the local model is down. Fix = give `local-reason` a **local-only** fallback chain (fail closed). The router isn't deployed, so there's no live exposure today — but the marketing promise "nothing leaves the box" is already printed on every Statement, so this must be closed before the router goes live **and** before that promise is repeated anywhere customer-facing.

### Blockers

- **#1 — t630 access is *the* Phase-1 critical path.** Items 1 and 2 above, plus the P2 security cleanup (TD-01 stale WG peers, TD-02 rotate the exposed Windows key), all require SSH to `192.168.1.118`. **Bundle all of it into a single box visit** — the marginal cost of doing security cleanup while you're already in is near zero, and TD-02 (exposed key) is a real one. If founder access to the box is intermittent, *that scheduling problem is the actual bottleneck for the whole company*, and it deserves to be treated as a calendar commitment, not a someday.

### The conservative / visionary tension worth naming

- **Conservative pole:** Phase 1 is dead simple and the discipline is correct — deploy boring nftables, collect real numbers, render one honest Statement, get 3 people to pay. Don't touch the app, don't touch Azure, don't add stack surface. The roadmap's "must not build" list is institutional memory protecting you from burning time on surface before liquidity. Hold this line.
- **Visionary pole:** The Odin/LangGraph orchestration layer (`10-ai-orchestration/langgraph-router/`) is a genuinely interesting AI-native homelab direction — a privacy-gated supervisor mesh is a real moat *if* it ships. But it is **three P3 deploys deep behind a gateway that isn't up**, and it is *not* what proves 3 customers exist. The honest read: it's a great Phase-2/3 asset being built ahead of its gate. That's fine as exploration — just don't let it consume box-visit time that items 1–2 need first.

The CEO decides which pole to inhabit on any given day. My recommendation for *this* week: inhabit the conservative pole hard. One box visit, three deploys, one Statement.

### No new budget impact this review
Nothing here requires spend. The pending **business** decisions (pricing validation, contractor-vs-employee classification, "guild-certified" vetting standard) all need ZORT and/or legal — the last two carry legal-fee impact and are flagged in the open-decisions table. They don't block the three technical items above, so they shouldn't pull focus this week.

Now updating `portfolio.md` with today's date and status changes.
