# NARF — review — 2026-06-21 — pass 1

I'll run the review. Let me pull the current state of the two files most likely to have moved since last review (2026-06-17): the CHANGELOG and the live config behind TD-14.

Confirmed: nothing has shipped since 2026-06-07 (CHANGELOG unchanged), and TD-14 is still live in the config exactly as described. Here's the review.

---

# Portfolio Review — 2026-06-21

## State check
- **CHANGELOG:** last entry still 2026-06-07. **Fourth review cycle with no new entry.** Zero real Statements produced.
- **TD-14:** verified live again. Line 100 of `config.yaml`: `local-reason: ["cloud-gpu-reason", "cloud-overflow"]`. A `sensitive` task routed to `local-reason` fails OPEN to Claude cloud (`cloud-overflow` = `anthropic/claude-opus-4-8`) if the local model is down. The file's own comments claim the opposite three times. The guarantee only exists in the un-deployed LangGraph gate.
- **Phase gate:** still **0/5**.

## Top 3 actionable now

**1. Close TD-14 — fail closed on the privacy fallback. Today. No box needed.**
File: `localDNS/10-ai-orchestration/config.yaml`, the `fallbacks:` block (~line 98).
Change:
```yaml
    - local-reason: ["local-smart", "local-fast"]   # was: ["cloud-gpu-reason", "cloud-overflow"]
```
Rationale: this is the single P1 item fixable without t630 access — a one-line edit. A false privacy claim is worse than no claim; right now the config asserts a guarantee it doesn't enforce. Note the trade-off honestly: failing closed means a heavy-reasoning request *errors* when the local box is down rather than silently completing on cloud — that is the correct behavior for a privacy product. Do it today.

**2. Schedule ONE t630 session and bundle every box-dependent item into it.**
Repos: `localDNS` + `customers`. SSH `192.168.1.118`. Runbook: `docs/statements/tools/collect/README.md`.
One operator visit clears: load nftables ruleset (`sudo nft -f nftables-accounting.nft`), schedule `populate_sets.py --apply` (6h cron) + `collect_stats.py` (nightly cron), produce the first real `stats.json` for HH-0001, and fold in TD-01 (remove/ID WG peers 10.8.0.4–6) + TD-02 (rotate exposed laptop key). This single trip unblocks TD-03, TD-08, TD-05, and the first real Statement. **This is the actual Phase-1 critical path** — see Blocker #1.

**3. Decide what the $50 dues *unlock*** (decision, not work).
Repo: `MARKETING`. Amount is set (ADR-005); inclusions are still `CHANGE_ME`-shaped and block an honest operator pitch. NARF recommendation: dues buy (a) Alliance listing + match priority, (b) shared tooling tier, and (c) explicitly **NOT** bonding/background-check coverage yet — that is tied to the unresolved contractor-vs-employee classification. **Budget + compliance impact — ZORT should weigh in** before we imply any coverage we don't carry.

## Blockers

1. **t630 access is the true reason Phase 1 isn't closing.** Four review cycles, no real Statement, because everything real is downstream of one SSH session. **If founder access is intermittent, that is the #1 business risk to Phase 1 — naming it to the CEO as such.** It is not a backlog problem.

2. **Two human-only decisions with budget/compliance impact:** contractor-vs-employee (lawyer) and "guild-certified" vetting standard (legal). Both gate Stage 09/10; the classification answer also gates the dues-inclusions decision. CEO + ZORT.

3. **GitHub issue creation still blocked** (`403 Resource not accessible by integration` — lacks `issues:write` on `localDNS`). The `issues` session mode cannot function. **Action for CEO:** grant the integration issue-write scope, or file the TD-14 and t630-session issues manually. This has persisted since last review — flagging it again because it silently neuters one of my modes.

---

Two poles worth holding on TD-14, since the CEO decides which to inhabit:
- **Conservative:** ship the one-line fail-closed fix now; it makes the config honest *today* with zero deploy risk. This is the boring, correct move.
- **Visionary:** the deterministic privacy gate (Heimdall, in the LangGraph supervisor) is the *real* architecture — a single failover-layer edit is a patch, not the guarantee. The full Odin orchestration layer makes config-level fallbacks irrelevant.

Both are true. The tension resolves by doing the conservative fix now anyway — it costs nothing and the visionary path isn't deployed. Don't let "the real fix is the supervisor" become a reason to leave a false claim live for another cycle.

Now updating portfolio.md with today's date and status changes.
