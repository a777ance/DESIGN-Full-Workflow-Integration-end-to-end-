# NARF — review — 2026-06-04 — pass 1

I'll review the portfolio. Let me read the key files to ground my assessment in current state.

The DESIGN README isn't at the expected path. Let me check the DESIGN repo's actual structure and the statements directory to verify deployment reality.

Both expected paths are missing. Let me try a couple of alternate locations to confirm the statement artifacts and DESIGN README exist somewhere.

I now have enough to ground the review. A key finding: the statements README contradicts the portfolio/context state on the nftables layer — that changes today's picture materially.

---

# Portfolio Review — 2026-06-04

## Key finding that changes the picture

The portfolio and `localDNS/context.md` both list the **nftables volume populator as "scaffolded, not deployed"** and treat it as the gating P1 (TD-03, TD-08). But `localDNS/docs/statements/README.md` shows the work is **further along than the tracker says**: both the ruleset (`nftables-accounting.nft`) *and* the IP-set populator (`populate_sets.py`) now ship in the repo, with a runbook in `tools/collect/README.md`. The README's own words: *"Built — run it on the box."*

So the blocker is no longer "build the populator." It's **"SSH to the t630 and run the runbook."** That's an afternoon of execution, not an engineering effort. The tracker is making a done-but-undeployed item look like open development work. I'm correcting that today.

## Top 3 actionable items right now

**1. Deploy the nftables accounting layer to the t630 (TD-03 / TD-08).**
This is the single highest-leverage move because three other things hang off it: per-category GB in statements (TD-08), PWA device testing on real numbers (TD-13), and the first real Statement. The code exists. Action: SSH `192.168.1.118`, follow `localDNS/docs/statements/tools/collect/README.md` — load `nftables-accounting.nft`, schedule `populate_sets.py` via cron, verify counters increment. Then re-run `collect_stats.py` (live, not `--sample`) and confirm volume sections are real.

**2. Generate the first real end-to-end Statement for one real household.**
This is the actual Phase 1 gate item, and it's gated on two things: #1 above (real volume data) and a **client data file**, which is supposed to come from the business side. Action: run the documented pipeline (`collect_stats.py` → `compose.py` → `generate_client.py`) against one real home, *scoped honestly* — if cohort data isn't ready, ship the statement without the "How You Compare" section rather than printing invented peer averages (the README explicitly warns against this on "a kept document"). Don't wait for cohort data to ship a real statement.

**3. Lock the member dues amount (TD-09).**
This is the only pure-business P1 and it's the cheapest to clear — it needs a decision, not work. It blocks Stage 09 onboarding and the operator pitch, and it's been sitting as `CHANGE_ME`. My recommendation: **pick a number this week** and write it into `MARKETING/README.md`. Given the unit-economics sketch (10 homes ≈ $3,600/yr to the operator), dues in the **$40–60/mo** range per operator are defensible against "what the guild gives you." It doesn't need to be perfect — it needs to exist so the downstream stages unblock. A wrong-but-validated-with-pilots number beats a blank.

## Blockers surfaced

- **Hard external blocker (both #1 and #2):** physical/SSH access to the t630 at `192.168.1.118`. Nothing in the Statement pipeline becomes real without it. Everything else in Phase 1 is downstream of this one access dependency. If founder access to the box is intermittent, that's the real critical path — flag it.
- **The "client data file" source is ambiguous.** Context says it comes "from Stage 05/08 in DESIGN," but I **could not read the DESIGN README** (`DESIGN-Full-Workflow-Integration-end-to-end-/README.md` — file not found) or `portfolio.md` at its own documented path. The hub repo's own files aren't where the spoke context files say they are. This is a one-source-of-truth integrity problem: if the hub can't be located, "Stage 08 master list" can't function as the single source of business facts. **Decision: verify the DESIGN repo's actual layout before the next session and fix the path references in the spoke context files.** I'm logging this as a blocker rather than guessing.
- **Cohort data (TD-07) is being treated as a blocker on the Statement.** It shouldn't be. Decision: it blocks *one section*, not the artifact. Scope the first real statement to what Pi-hole + Uptime Kuma + nftables already prove, and the gate clears without it.

## Things I'm explicitly NOT doing

- No Phase 2 / app work — gate is not cleared (0 of 5 boxes).
- TD-01/TD-02 (WireGuard peer cleanup, key rotation) stay P2 — security hygiene, not Phase-1-blocking. They need physical access anyway; bundle them with the t630 visit in #1.

Let me update the portfolio to reflect the corrected nftables status, the new top-3 ordering, and the DESIGN-path integrity blocker.

Review complete. Summary of what I did and changed:

**Top 3 actionable items (in order):**
1. **Deploy nftables accounting to the t630** — the highest-leverage move. The code is already shipped; this is execution, not engineering.
2. **Generate the first real Statement for one household** — the actual Phase 1 gate item, scoped honestly (omit cohort section rather than print fake averages).
3. **Lock member dues** — the cheapest P1 to clear; needs a decision, not work. Recommended $40–60/mo.

**Blockers surfaced:**
- **t630 SSH access** is the real Phase-1 critical path — both top items and the security cleanup depend on it.
- **DESIGN hub files aren't at their documented paths** — I couldn't read the DESIGN README or its portfolio.md. This is a one-source-of-truth integrity problem and needs fixing before "Stage 08 master list" can be trusted.
- **Cohort data was miscast as a Statement blocker** — corrected: it blocks one section, not the artifact.

**Status corrections written to portfolio.md:**
- Reclassified the nftables work from "scaffolded, not deployed" to "code shipped, ready to deploy" — the tracker was making a done item look like open development.
- Logged two new decisions and three active blockers with concrete next actions.

One thing I'd flag for your attention: the DESIGN path issue is worth resolving before the next session, because two of your three priorities ultimately route through "where does the client data file come from," and that answer lives in a hub I currently can't locate. Want me to dig into the DESIGN repo's actual structure now, or open issues for the top items?
