# NARF — review — 2026-06-14 — pass 1

I'll run the review. Let me pull the live ground-truth files first — the CHANGELOG and the key open items — so I'm reviewing reality, not the last snapshot.

Both source documents confirm the snapshot is accurate. Here's the review.

---

## Portfolio Review — 2026-06-14

### Top 3 actionable items right now

**1. Close TD-14 (privacy fallback gap) — it's a code edit, not a deployment, and it's the only P1 that doesn't need the t630.**
Everything else on the P1 list is blocked behind box access. This one isn't. Right now `config.yaml` gives the `local-reason` tier a fallback chain that includes `cloud-overflow` (Claude cloud), so a sensitive prompt can leak to a cloud provider if the local model is down — directly contradicting the privacy promise printed on every Statement. Fix: edit `10-ai-orchestration/.../config.yaml` so `local-reason`'s fallback is **local-only (fail closed)**, then run the dispatcher's `--selftest` to confirm. This is the single highest-leverage thing doable without leaving the keyboard.

- **Conservative pole:** Fail closed. A privacy guarantee you can't enforce at the failover layer isn't a guarantee — it's a liability the moment you print it on a customer artifact. Fix it before any Statement ships.
- **Visionary pole:** The real architecture here is a hard cryptographic/network boundary — sensitive-tagged traffic that *physically cannot* egress, not a config flag trusting LiteLLM to behave. That's the privacy-first edge-inference moat worth building toward. But that's Phase 2+; today, fail closed.

**2. Generate the first real end-to-end Statement for HH-0001 (Dave) — the box-dependent critical path.**
Per the collect README, **three of four data sources are real the moment the box is up** (Pi-hole FTL, Uptime Kuma `/metrics`, `wg show`). Only per-category volume needs nftables. So you do *not* have to deploy nftables to ship an honest first Statement — the generator self-scopes and omits the volume sections (ADR, commit `8dcb7fe`). Concrete next action: SSH to `192.168.1.118`, run `collect_stats.py --out` for the running month, then render via `--data-dir`/`--out-dir` into the `customers` repo. One honest Statement beats a complete fake one.

**3. Deploy the nftables accounting layer to the t630 — bundle it into the same SSH visit.**
This is execution, not engineering (ADR 2026-06-04). Runbook is exact: `sudo nft -f nftables-accounting.nft`, then `sudo python3 populate_sets.py --apply`, then the two cron lines (6-hourly populate, nightly collect). Lights up the donut + Household Profile sections and unblocks TD-03, TD-08, TD-13. **While you're on the box, clear TD-01 and TD-02** (identify/remove WG peers 10.8.0.4–6; rotate the exposed Windows laptop key) — same access, defense-in-depth.

### Blockers

- **t630 access (192.168.1.118) is the entire Phase-1 critical path.** Items 2 and 3, plus TD-01/02/05, all sit downstream of one SSH session. If founder box-time is intermittent, *that* is the bottleneck — not engineering. This is why I've reordered TD-14 to #1: it's the only P1 progress available between box visits.
- **No budget/spend blockers** in my lane today. Item 3's cron and item 1's edit cost nothing. Flag: when you eventually stand up the AI gateway (P3), `cloud-gpu-reason` and the cloud tiers require an Anthropic API key — **that has a budget impact, ZORT should weigh in** before any key is provisioned. Not actionable now; noting it so it doesn't surprise anyone.

### Status changes I'm recording
- TD-14 reframed as the top actionable (off the critical path, fail-closed fix).
- Clarified that the first real Statement does **not** require nftables — only `collect_stats.py` on the box (per the collect README). nftables enriches it, doesn't gate it.
- Folding TD-01/TD-02 into the same box visit as items 2 and 3.

Let me update the portfolio.
