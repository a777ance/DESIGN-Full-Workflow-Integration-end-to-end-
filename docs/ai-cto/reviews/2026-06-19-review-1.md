# NARF — review — 2026-06-19 — pass 1

I'll run the review. Let me verify the live state of the two highest-leverage technical items before I commit to recommendations — TD-14 (the privacy fallback config) and the t630-dependent deploy chain.

Both confirmed against live files. TD-14 is real and the fix is exactly as described. The t630 runbook is complete and ready to execute. Here's the review.

---

## Portfolio Review — 2026-06-19

**Phase 1 gate: 0/5. No CHANGELOG entry since 2026-06-07 — now 12 days, four review cycles, zero real Statements.** I'm going to stop calling this a backlog problem in increasingly polite language. It's a decision-and-access problem, and I'll name it bluntly below.

### Top 3 actionable items right now

**1. Close TD-14 — fail the LLM-router closed. (`localDNS/10-ai-orchestration/config.yaml`, ~3-line edit, no box access needed.)**

Re-verified against the live config. The leak is exactly here:

```yaml
fallbacks:
  - local-reason: ["cloud-gpu-reason", "cloud-overflow"]   # ← the gap
```

A `sensitive`-tagged task pinned to `local-reason` will fail OPEN to `cloud-overflow` (Claude cloud, `anthropic/claude-opus-4-8`) the moment the local distill is unreachable. The config's own comments assert the opposite *three times* — "the privacy-preserving default," "a `sensitive` task never routes here," "a cloud→cloud fallback can't leak a private lookup." That last claim is the dangerous one: it's only true if the *entry* into the cloud chain is blocked, and `local-reason → cloud-overflow` is exactly that entry. The guarantee currently lives only in the un-deployed LangGraph gate (`allow_cloud=False`), which is *not* enforced at the LiteLLM failover layer. Config and deployed reality disagree.

**The fix:**
```yaml
  - local-reason: ["local-smart", "local-fast"]   # fail closed: local-only, never cloud
```
Then update the three lying comments. A false privacy claim is worse than no claim — it's the one thing that turns a "we run it and prove it" pitch into a liability. **This is the only P1 fixable without the t630. Do it today.**

**2. Schedule ONE t630 session and bundle every box-dependent item into it. (`localDNS` + `customers`, SSH `192.168.1.118`.)**

Not five tasks — one operator visit. The runbook is complete and idempotent (`docs/statements/tools/collect/README.md`). In one trip:
- `sudo nft -f nftables-accounting.nft` (load counters) → TD-03
- `sudo python3 populate_sets.py --apply` + install the **6h cron** and **nightly `collect_stats.py` cron** → TD-08
- produce the first real `stats.json` for HH-0001 → unblocks the first real Statement end-to-end
- while you're in there: remove/identify WG peers 10.8.0.4–6 (TD-01) and rotate the exposed laptop key (TD-02)

Note from the runbook: 3 of 4 data sources (Pi-hole, Uptime Kuma, `wg show`) are **already live** — only volume-by-category is missing. So this one session clears the path to a Statement that's *mostly real today*, with one honestly-labeled sample section. That's a shippable artifact.

**3. Decide what the $50 dues *unlock*. (`MARKETING` + ZORT — a decision, not work.)**

Amount is locked (ADR-005); inclusions are still `CHANGE_ME`-shaped, which blocks an honest operator pitch. My recommendation stands: dues buy **(a) Alliance listing + match priority, (b) shared tooling tier, and explicitly (c) NOT bonding/background-check coverage yet** — that's tied to the unresolved contractor-vs-employee question. **Budget + compliance impact — ZORT should weigh in before we imply any coverage we don't carry.** Implying bonding we don't hold is a fiduciary problem, not just a marketing one.

---

### Blockers — surfaced honestly

1. **t630 access is THE Phase-1 critical path, and intermittent access is the #1 business risk to Phase 1.** Everything real is downstream of one SSH session. Four review cycles of "schedule the session" is the actual root cause of 0/5. **CEO: if box access is genuinely intermittent, that's a structural risk worth naming out loud — not a to-do that keeps rolling.**

2. **Two human-only decisions with budget/compliance teeth:** contractor-vs-employee (lawyer) and "guild-certified" vetting standard (legal). Both gate Stage 09/10; the first also gates the dues-inclusions decision. CEO + ZORT.

3. **GitHub issue creation was blocked last review (`403 Resource not accessible by integration` on `localDNS` — missing `issues:write`).** I have not re-tested the scope this session. The two ready-to-file issues (TD-14 fix; t630 deploy session) remain undrafted as issues. **CEO: grant the integration `issues:write`, or I can dry-run them so you paste them manually.** Until fixed, `issues` mode can't function.

---

### The metamodern note (one pole worth holding)

The conservative read: TD-14 proves the un-deployed Odin supervisor is carrying a safety guarantee that the *deployed* layer (LiteLLM) doesn't enforce. Boring best practice says **the privacy guarantee must live at the lowest deployed layer** — config-level fail-closed — not in an orchestration tier that isn't running. Fix #1 does exactly that.

The visionary read: this is also a signal that the Odin/LangGraph privacy gate *is the right architecture* — a single deterministic place where "sensitive never crosses the Bifröst" is enforced once instead of being re-litigated in every fallback chain. Both are true. Today, inhabit the conservative pole (fix the config). When the gate deploys, the elegant version becomes load-bearing. Don't let the elegant future excuse an open leak in the boring present.

Now updating portfolio.md.
