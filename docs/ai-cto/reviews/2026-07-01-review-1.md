# NARF — review — 2026-07-01 — pass 1

I'll review the portfolio. Let me first verify the two most consequential claims from the last review — TD-14 against the live config, and the t630-dependency chain — before I re-assert them.

Both claims verified against live files. Here's the review.

---

# Portfolio Review — 2026-07-01

## What's actually true right now

- **TD-14 is real, confirmed a third time against the live `config.yaml`.** The fallback map still reads `local-reason: ["cloud-gpu-reason", "cloud-overflow"]`. A sensitive-tagged task pinned to `local-reason` will fail *open* to Claude cloud (`cloud-overflow` → `anthropic/claude-opus-4-8`) the moment the local model is down or cooled out. The config's own comments assert the opposite ("a `sensitive` task never routes here") — but that guarantee lives only in the un-deployed LangGraph gate, **not** in the LiteLLM failover layer that's actually running the routing. **A false privacy claim is worse than no claim.** This is the single P1 fixable without box access.

- **CHANGELOG's newest entry is still 2026-06-07.** Nothing has shipped in ~three weeks. That's now *four* review cycles with no real Statement. This is not a backlog problem — it is a **cadence** problem: everything real is downstream of one SSH session to the t630, plus two decisions that need humans.

- **The t630 runbook is clean and ready.** I re-read `collect/README.md` — the volume layer standup is genuinely a bundled, idempotent, one-visit job: `nft -f`, `populate_sets.py --apply`, two cron lines, one `collect_stats.py` run. No engineering left; it's an operator visit.

---

## Top 3 actionable items right now

**1. Close TD-14 — fail closed on the LLM-router privacy fallback.** `localDNS/10-ai-orchestration/config.yaml`, one line:
```yaml
    - local-reason: ["local-smart", "local-fast"]   # was: ["cloud-gpu-reason", "cloud-overflow"]
```
Drop `cloud-gpu-reason` and `cloud-overflow` from any chain a sensitive task can reach. `cloud-gpu-reason` is itself a rented-GPU tier that spills to `cloud-overflow` when the pod is off (its normal state) — so leaving it in the chain is a cloud leak by default. Chain `local-reason` to the two local tiers only. **3-line edit, no box access, do it today.** Also update the two config comments that currently *assert* a guarantee the running config doesn't keep — the code and the comment must agree.

**2. Schedule ONE t630 session; bundle every box-dependent item into it.** SSH `192.168.1.118`. Runbook: `localDNS/docs/statements/tools/collect/README.md`. In one visit:
- `sudo nft -f nftables-accounting.nft` (load counters) → TD-03
- `sudo python3 populate_sets.py --apply` + the two cron lines (6h populate / nightly collect) → TD-08
- produce the first real `stats.json` for HH-0001 → Phase-gate item #5
- clean up WG peers 10.8.0.4–6 (TD-01) + rotate the exposed laptop key (TD-02)

One trip unblocks four tech-debt items, two phase-gate checkboxes, and the first honest Statement. **If founder box-access is intermittent, that intermittency IS the #1 business risk to Phase 1 — I'm naming it to the CEO as such.**

**3. Decide what the $50 dues *unlock*.** `MARKETING`, decision not code. Amount is set (ADR-005); inclusions are still `CHANGE_ME`-shaped and block an honest operator pitch. My recommendation: dues buy **(a)** Alliance listing + match priority, **(b)** shared tooling tier, and **(c)** explicitly **NOT** bonding/background-check coverage yet — that's chained to the unresolved contractor-vs-employee classification. **Budget + compliance impact — ZORT should weigh in before we imply any coverage we don't carry.** Implying bonding we don't hold is a fiduciary and legal exposure, not just a marketing choice.

---

## Blockers (surfaced, unchanged, and one still open)

1. **t630 access is the Phase-1 critical path.** Four cycles, no Statement, because everything real is one SSH session away. Bundle it (item #2).
2. **Two decisions need humans, both with budget/compliance impact:** contractor-vs-employee (lawyer) and "guild-certified" vetting standard (legal). ZORT + CEO. The dues-inclusions decision partly depends on the classification answer.
3. **GitHub issue creation was blocked last cycle** (`403 Resource not accessible by integration` — missing `issues:write` on `localDNS`). **Action for CEO:** grant the scope, or the `issues` mode stays non-functional and TD-14 / t630 issues can't be filed automatically.

---

## The tension worth naming (metamodern note, for the CEO — not a recommendation to act on now)

**Conservative pole:** TD-14 is exactly why boring, fail-closed defaults exist. A privacy product's entire value is the guarantee; a config that fails *open* to the cloud silently voids the promise you print on every Statement. Fix it, keep the whole orchestration layer un-deployed until liquidity is proven, ship static HTML. This is correct for Phase 1.

**Visionary pole:** the Odin/LangGraph supervisor — a deterministic privacy gate (Heimdall) enforcing local-only routing at the *architecture* level rather than a hand-maintained fallback map — is genuinely the right long-term shape. It's what makes TD-14-class bugs structurally impossible. But it is Phase-2-plus surface, and building it now to fix a 3-line config bug would be elegance in service of nothing. **Both true. The config fix today; the architecture when there are customers to protect with it.**

Now updating portfolio.md.
