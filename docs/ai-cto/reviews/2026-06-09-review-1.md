# NARF — review — 2026-06-09 — pass 1

I'll run a portfolio review. Let me pull the current state of the key files to verify what's real versus documented before I update anything.

The runbook confirms what the portfolio claims. Let me verify the two things most likely to have drifted: the privacy-gap config (TD-14, the only P1 with a real safety implication) and whether the dues/pricing decisions are actually written into MARKETING.

The config path drifted — TD-14 references `config.yaml` but the file isn't at the documented path. Let me check the actual orchestration directory structure before I assert the privacy gap is still live, since that's the only P1 with a safety implication.

Now I have ground truth. Two important findings worth flagging in the review:

1. **TD-14's path reference is stale** — the config is at `10-ai-orchestration/config.yaml`, not `langgraph-router/config.yaml`. The gap itself is real: line `local-reason: ["cloud-gpu-reason", "cloud-overflow"]` still fails *open* to cloud.

2. **But the LangGraph supervisor README claims this is already mitigated in code** — Heimdall pins `sensitive` tasks local and the Warden rewrites cloud→local. So the severity of TD-14 depends entirely on *which front door you use*. That's an honest nuance the portfolio doesn't capture, and it matters for the privacy promise printed on every Statement.

Let me write up the review.

---

# Portfolio Review — 2026-06-09

## Top 3 actionable items right now

**All three are unchanged from the last review, and that is itself the headline.** None require engineering. All are gated on one of two things: **t630 access** or **a CEO decision**. Nothing technical is actually blocking Phase 1 — execution and decisions are.

### 1. Deploy the nftables accounting layer to the t630 — `localDNS`
**This is the keystone.** It unblocks TD-03, TD-08, TD-13, and the volume/profile sections of the first real Statement. The runbook (`docs/statements/tools/collect/README.md`) is concrete and idempotent:
```bash
sudo nft -f nftables-accounting.nft          # load ruleset (counters at zero)
sudo python3 populate_sets.py --apply        # program the IP-sets
# then the two crons: populate every 6h, collect nightly
```
This is execution, not engineering. **Blocker: SSH/physical access to `192.168.1.118`.**

### 2. Generate the first real end-to-end Statement for HH-0001 (Dave) — `localDNS` + `customers`
The generator self-scopes (omits any section without honest data), so month one is honest by construction even before nftables lands. **The only remaining step is running `collect_stats.py` on the box** for real Pi-hole / Uptime Kuma / WireGuard figures — three of four sources are real the moment the box is up. Deploying nftables (item 1) additionally lights up the volume + profile sections. **Same blocker: t630 access.**

### 3. Decide what the $50/mo dues *unlock* — `MARKETING`
The dues *amount* is set (ADR-005). What's still `CHANGE_ME`-shaped is the **inclusions**: tooling tier, match priority, bonding/background-check coverage. This is a decision, not work, and it blocks Stage 09 onboarding and the operator pitch from being concrete. 

**My recommendation:** dues buy three things — (a) the toolchain + generator access, (b) match priority in the Alliance directory, (c) *listing* as guild-certified once vetted. Hold bonding/background-check **out** of the base dues for now — that has a budget impact (per-operator cost) and is gated on the vetting-standard decision, which needs legal. **Flag: bonding/background checks are a ZORT + legal question, not something to fold into dues unilaterally.**

---

## Blockers

**Critical path — unchanged and hardening:** Every real Phase-1 deliverable (items 1, 2, plus security items TD-01, TD-02) is downstream of **t630 SSH access at `192.168.1.118`**. This review marks the **third consecutive review** where the top two items have not moved, and the cause is the same each time. 

**Fiduciary flag, stated plainly:** the bottleneck is not technical and never was — it's a single recurring access window. If founder access to the box is intermittent, *that* is the Phase-1 constraint, full stop. My recommendation: bundle everything that needs the box into **one deliberate session** — nftables deploy, `collect_stats.py` run, rotate the exposed Windows WireGuard key (TD-02), and identify/remove the stale peers 10.8.0.4–6 (TD-01). One visit clears four items.

**New finding this review — a documentation gap on the privacy guarantee (TD-14):**
I verified the config against the actual files. Two corrections to the tracked state:

- **TD-14's path is stale.** It points at `langgraph-router/config.yaml`; the real file is `10-ai-orchestration/config.yaml`. The fallback `local-reason: ["cloud-gpu-reason", "cloud-overflow"]` is still present — **the gap is real**.
- **But severity is now conditional, and the portfolio doesn't say so.** The `langgraph-router` supervisor (Odin/Heimdall) README claims a deterministic gate that pins `sensitive` tasks local and rewrites cloud→local *before* LiteLLM failover is ever reached — testable offline via `--selftest`. **So:** through the *supervisor* front door, sensitive prompts are fail-closed in code. Through the *flat dispatcher / raw LiteLLM* front door, the fail-open fallback still applies.

This nuance matters because the privacy promise is **printed on every Statement** ("nothing leaves the box"). Neither path is deployed, so there's no live exposure today — but the fix and the claim must be reconciled *before* the AI layer goes near a real household. **Both poles are true and the tension is the useful part:**

- **Conservative:** fail closed at the config layer regardless of which front door is used. Give `local-reason` a local-only fallback (e.g. `["local-smart"]`). Defense-in-depth means the LiteLLM layer shouldn't *depend* on the supervisor being in the path. One line, no deploy needed.
- **Visionary:** the supervisor *is* the architecture — a deterministic privacy gate above a dumb front door is the right long-term shape, and it's already built and self-tested. Lean into it; make Odin the *only* sanctioned door and retire raw dispatcher access.

I'd do the conservative one-line fix now (it's free and it protects the operator who wires this up at 11pm without reading the supervisor docs) **and** keep building toward the supervisor-as-sole-door. The CEO picks the pace on the second.

This is P3-priority by *timeline* (nothing AI is deployed) but P1 by *principle* (it's the privacy claim). I'm leaving it P1 in tech-debt with a corrected path and a note that the fix is one line.

---

## Status changes I'm recording

- TD-14: corrected the file path; noted the supervisor mitigates it on one path but the flat path still fails open; fix is a one-line config change.
- Flagged the t630-access bottleneck as a recurring (3rd-review) pattern, not a one-off.
- Item 3 reframed from "lock dues" (done) to "decide dues inclusions" (the actual open piece).

Let me update the portfolio.
