# NARF — review — 2026-06-15 — pass 1

I'll run the review. Let me pull the current state of the critical files before forming conclusions — the privacy-fallback gap (TD-14) and the t630 deploy status are the two things I most want to verify against source.

The config moved when the stage was renamed. Let me find it.

Both source facts confirmed. The TD-14 privacy gap is **still live and verbatim**: in `config.yaml`, `local-reason` falls back to `["cloud-gpu-reason", "cloud-overflow"]` — both cloud. The fix is a one-line change. And the collect runbook confirms #1/#2 are pure deployment, not engineering.

Here's the review.

---

# Portfolio Review — 2026-06-15

## State of play

Phase 1, "Prove liquidity." The gate is unchanged: **0 of 5 checkboxes ticked**, and every real Phase-1 outcome is downstream of one thing — getting onto the t630 (`192.168.1.118`). Eight days since the last update and the box still hasn't been touched. That's the headline, and I'll say it plainly: **the project is not blocked by engineering. It's blocked by an SSH session that hasn't happened.**

## Top 3 actionable items right now

**1. Get on the t630 and deploy the nftables accounting layer.** *(P1 — TD-03/TD-08)*
This is execution, not engineering. The runbook is exact (`docs/statements/tools/collect/README.md`):
```bash
sudo nft -f nftables-accounting.nft
sudo nft list table inet a777acct          # verify sets + counters exist, all zero
sudo python3 populate_sets.py --apply      # program the IP-sets
```
Then schedule the two cron lines (populate every 6h, collect nightly). Bundle the P2 security cleanup into the *same* session — rotate the exposed Windows WireGuard key (TD-02) and identify/remove WG peers `10.8.0.4–6` (TD-01). One visit clears four debt items.

**2. Fix the LLM-router privacy fallback (TD-14) — one line, do it before any next deploy.** *(P1)*
I verified this against `10-ai-orchestration/config.yaml` just now. It is still wrong:
```yaml
- local-reason: ["cloud-gpu-reason", "cloud-overflow"]   # both CLOUD
```
A sensitive prompt routed to `local-reason` fails *open* to Claude cloud if the local model is down. The fix is to fail **closed**:
```yaml
- local-reason: ["local-smart"]        # local-only; no cloud egress for sensitive work
```
This costs nothing and the orchestration layer isn't deployed yet — so fixing it now means we never ship the leak. **Do this regardless of the t630 visit; it's a repo edit.** Until it's done, the "nothing sensitive leaves the box" promise printed on every Statement is not true for the AI layer.

**3. Generate the first real end-to-end Statement for HH-0001 (Dave).** *(P1)*
The moment #1 is done, run `collect_stats.py` on the box for real figures, then render via the generator's `--data-dir`/`--out-dir` into the private `customers` repo. The generator self-scopes (omits sections without honest data), so a real Statement is achievable *today* on the three live sources (Pi-hole, Uptime Kuma, WireGuard) even before nftables — but volume + profile sections only light up once #1 lands. This is the artifact that makes the QR referral loop and the whole pitch real.

## Blockers

- **#1 — t630 access is THE critical path.** Items 1 and 3 above, plus TD-01/TD-02, are all downstream of one SSH session. If founder access to `192.168.1.118` is intermittent, *that* is the real bottleneck — name a date.
- **TD-14 is a stated-promise gap, not just tech debt.** Flagging it with fiduciary weight: we print a privacy guarantee we don't currently honor in the AI path. Cheap to fix, embarrassing to ship. No budget impact.
- **TD-06 (DESIGN Stage 11 automations not wired)** remains P1 and untouched — every cross-stage transition is manual re-entry. Not on the critical path for the first Statement, but it's the silent tax on every customer you add. Lower than #1–3 only because N=1.

## Two open decisions worth a nudge (not work — decisions)

- **CRM tool:** CEO's hypothesis (Sheets → Airtable free → HubSpot) is sound. My read: **start on Google Sheets today**, no friction, and confirm the Airtable free tier (1,200 records, Zapier/Make webhooks) clears the Stage 11 automation plan before committing. No blocker found, but I haven't yet read `08-client-list-and-crm/schema.md` against Airtable's automation limits — I'll do that in a focused pass rather than hand-wave it here.
- **Pricing validation** stays open by definition until the first cohort renews — not actionable yet.

The two-pole tension worth naming: **conservative** — ship the dull static Statement off three working data sources *now*, prove a human will pay, don't let the AI-orchestration cathedral (Odin, Heimdall, the whole pantheon) distract from a paying customer. **Visionary** — that orchestration layer is a genuine moat if A777ance becomes a guild of community-run resolvers. Both true. But in Phase 1, the static Statement is the only one that earns money. The CEO decides where to spend the next session; my recommendation is the t630.

Let me update the portfolio.
