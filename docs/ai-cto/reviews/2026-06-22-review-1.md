# NARF — review — 2026-06-22 — pass 1

I'll run the review. Let me first verify the live state of the two items most likely to have changed — TD-14's config and the CHANGELOG — before I assert anything stale.

Confirmed: TD-14 is still live and unfixed. The offending line is exactly as recorded:

```yaml
- local-reason: ["cloud-gpu-reason", "cloud-overflow"]
```

A `sensitive` task routed to `local-reason` can fail OPEN to `cloud-overflow` (`anthropic/claude-opus-4-8`, cloud) if the local model is down. The config's own comments assert the opposite — the privacy guarantee only exists in the un-deployed LangGraph gate, not at the LiteLLM failover layer where it actually matters. CHANGELOG also unchanged since 2026-06-07. Nothing material shipped since last review. Let me update the portfolio and present the review.
